# todo change for bearish options when time comes

trade bot:
    if : Pair is already trying to handle place sell in parallel, returning...
    and : sell fails (eg -105)
    then: another sell is never placed
    solution tried: this.is_handling_place_sell = false; inside place_sell_error

    Sometimes PARTIALL FILL happens after FILLED, triggering an useless cancel of old for place of new order

    Future: change its not 70 limit orders at all time but one order placed when price reached
            so bigger positions can be placed

    sometimes its having a sell orders with unassed small available balance such as min notional size

    sometimes WS response of fills is super spammy and causing multiple parralles handle_sell:
        solution tried:

        if (this.is_handling_place_sell) {
             print(this.pair, 'Pair is already trying to handle place sell in parallel, returning...');
             return;
        }

    what about place_sell_order not having conditions, no retry?


backtest:
    high priority:
        - Test On Balance Volume divergence (as short slope of price vs short slope of obv) (seems very good)
        - optimise for "short time" up-trend / side-way / down-trend, + dev?
        - Winning Trade holding time versus Losing Trade holding time

    medium priority:
        - dynamic pos sizes to obfuscate, and prob important to give more capital to performing pairs
        - simulate fill, instead of penalty, when buy exatly at low price, might not fill in reality for low liquidity pairs

    low priority:


    Window sized shorter during high volatility?
    average of short term model and medium-long term one

    take profit too low and fast?


other:
    Centraliser logs sans besoin connexion

    Check how many open orders, prevent bug like last time.... (pm2 startup too)

    should probably put recalc df into another vps and use database to share (1gb dfs probs?)

    make script for sell all and cancel all, run at start

    Update BTC balance


     Types: NEW CANCELED REJECTED TRADE EXPIRED
     "E": 1499405658658,            // Event time *
     "s": "ETHBTC",                 // Symbol *
     "c": "mUvoqJxFIILMdfAW5iGSOW", // Client order ID
     "S": "BUY",                    // Side *
     "o": "LIMIT",                  // Order type *
     "q": "1.00000000",             // Order quantity *
     "p": "0.10264410",             // Order price *
     "C": "null",                   // Original client order ID; This is the ID of the order being canceled *
     "x": "NEW",                    // Current execution type *    if status is FILLED, this will be TRADE
     "X": "NEW",                    // Current order status *      FILLED CANCELED NEW
     "r": "NONE",                   // Order reject reason; will be an error code. *
     "i": 4293153,                  // Order ID *
     "l": "0.00000000",             // Last executed quantity *
     "z": "0.00000000",             // Cumulative filled quantity *
     "L": "0.00000000",             // Last executed price *
     "n": "0",                      // Commission amount *
     "T": 1499405658657,            // Transaction time
     "t": -1,                       // Trade ID
     "O": 1499405658657,            // Order creation time
     "Z": "0.00000000",             // Cumulative quote asset transacted quantity
     "Y": "0.00000000"              // Last quote asset transacted quantity (i.e. lastPrice * lastQty)

if problem when calling py_2:
    have __init__.py
    have setup.py
    have backtester in path
    pip install --no-deps -e .


    problem keyerror or something when calcling dfs?
    find wich pair
    replace 15m df with backup one



'pkg_resources' missing:
    sudo apt-get install --reinstall python3-pkg-resources


Si probleme 'main' avec pip:
    python3 -m pip uninstall pip setuptools wheel
    sudo apt-get --reinstall install  python3-setuptools python3-wheel python3-pip

    python -m pip uninstall pip setuptools wheel
    sudo apt-get --reinstall install python-setuptools python-wheel python-pip